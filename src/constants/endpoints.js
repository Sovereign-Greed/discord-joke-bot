/** External HTTP endpoints (used by `src/api/`). */
export const CAT_API_URL = 'https://api.thecatapi.com/v1/images/search';
export const JOKE_API_URL = 'https://v2.jokeapi.dev/joke/Any?type=single';

/** [D&D 5e API](https://www.dnd5eapi.co/) — SRD reference data (no key). */
export const DND5E_API_BASE = 'https://www.dnd5eapi.co/api';

/** Origin for building links from API `url` paths (e.g. `/api/spells/fireball`). */
export const DND5E_ORIGIN = 'https://www.dnd5eapi.co';
