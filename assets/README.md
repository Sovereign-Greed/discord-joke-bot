## Assets

This folder is for optional images you want the bot to attach in embeds.

### `/roll`

- **`assets/roll.png`**: If this file exists, `/roll` will attach it and show it as the embed thumbnail.
- Leave it missing to disable roll images (no attachment sent).

